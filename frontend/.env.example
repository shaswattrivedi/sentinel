# SENTINEL Frontend Environment Variables
# Copy this file to .env.local and adjust as needed

# Backend API URL (defaults to localhost:4000 via Vite proxy)
VITE_API_BASE_URL=/api/v1

# ML Service URL (direct connection, bypassing proxy)
VITE_ML_API_BASE_URL=http://localhost:8000
